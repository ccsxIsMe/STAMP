# ============================================================
# Exp06: Macenko stain normalization for TCGA
#
# IMPORTANT: STAMP preprocessing does NOT support built-in
# Macenko normalization (no 'normalizer' field in config).
#
# Workaround: Use torchstain library to normalize TCGA tiles
# BEFORE feature extraction via a custom wrapper script.
# See scripts/macenko_preprocess.py for instructions.
#
# This experiment is SKIPPED in Phase 1.
# Revisit if Exp01-05 show significant domain shift.
# ============================================================
